{
  "name": "dmm-title-watcher",
  "version": "1.0.0",
  "description": "",
  "main": "handler.js",
  "scripts": {
    "test": "./script/create-dynamodb-test-tables.sh && sls offline -s test -P 3001 > /dev/null 2>&1 & sleep 5 && sls invoke test --stage test -t 60000"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@slack/web-api": "^5.0.1",
    "dmm.js": "^0.1.0",
    "dynamodb": "^1.2.0",
    "dynamodb-marshaler": "^2.0.0"
  },
  "devDependencies": {
    "aws-sdk": "^2.480.0",
    "serverless-mocha-plugin": "^1.10.0",
    "serverless-offline": "^5.4.4",
    "serverless-offline-dynamodb-streams": "^2.0.0"
  }
}
